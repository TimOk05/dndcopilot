services:
  dnd-copilot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - dnd-copilot-data:/app/.data

volumes:
  dnd-copilot-data:
